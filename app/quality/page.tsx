import Link from "next/link";
import { getSiteContent } from "@/lib/cms";
import { PageHero, SiteShell } from "../components/SiteShell";
import { QualityJourney } from "../components/QualityJourney";
export const dynamic = "force-dynamic";
export default async function QualityPage(){const {quality:c}=await getSiteContent();return <SiteShell><PageHero number={c.heroNumber} kicker={c.heroKicker} title={c.heroTitle} accent={c.heroAccent} intro={c.heroIntro}/><section className="quality-hero-image" style={{backgroundImage:`url(${c.heroImage})`}}><div><strong>{c.heroStat}</strong><span>{c.heroStatLabel}</span></div></section><section className="quality-principles page-shell"><div className="quality-principles-heading"><p className="eyebrow">{c.architectureEyebrow}</p><h2>{c.architectureTitle}<br/><em>{c.architectureAccent}</em></h2></div><div className="quality-principle-grid">{c.principles.map((p,i)=><article key={i}><span>{String(i+1).padStart(2,"0")}</span><h3>{p.title}</h3><p>{p.copy}</p></article>)}</div></section><QualityJourney copy={c}/><section className="documents-band"><div className="page-shell"><div><p className="eyebrow light">{c.documentsEyebrow}</p><h2>{c.documentsTitle}</h2></div><div className="document-list">{c.documents.map((item,i)=><span key={i}><b>{String(i+1).padStart(2,"0")}</b>{item}</span>)}</div></div></section><section className="certification-section"><div className="certification-heading page-shell"><div><p className="eyebrow">{c.certificationEyebrow}</p><h2>{c.certificationTitle}<br/><em>{c.certificationAccent}</em></h2></div><p>{c.certificationIntro}</p></div><div className="certification-marquee" tabIndex={0}><div className="certification-track">{[...c.certifications,...c.certifications].map((item,i)=><article className="certification-card" aria-hidden={i>=c.certifications.length} key={`${item.name}-${i}`}><div><img src={item.image} alt={i<c.certifications.length?`${item.name} logo`:""}/></div><span>{item.name}</span><small>{item.type}</small></article>)}</div></div><p className="certification-note page-shell">{c.certificationNote}</p></section><section className="quality-close page-shell"><h2>{c.closeTitle}</h2><p>{c.closeCopy}</p><Link href="/contact" className="button button-dark">{c.closeButton} <span>↗</span></Link></section></SiteShell>}
