import Link from "next/link";
import { getSiteContent } from "@/lib/cms";
import { PageHero, SiteShell } from "../components/SiteShell";
export const dynamic = "force-dynamic";
export default async function AboutPage() { const { about: c } = await getSiteContent(); return <SiteShell><PageHero number={c.heroNumber} kicker={c.heroKicker} title={c.heroTitle} accent={c.heroAccent} intro={c.heroIntro} /><section className="about-mosaic page-shell"><div className="about-photo about-photo-field" role="img" aria-label={c.primaryImageAlt} style={{ backgroundImage: `url(${c.primaryImage})` }} /><div className="about-pullquote"><span>“</span><p>{c.quote}</p></div><div className="about-photo about-photo-grain" role="img" aria-label={c.secondaryImageAlt} style={{ backgroundImage: `url(${c.secondaryImage})` }} /></section><section className="story-copy page-shell"><p className="eyebrow">{c.storyEyebrow}</p><div><h2>{c.storyTitle}</h2>{c.storyParagraphs.map((p) => <p key={p}>{p}</p>)}</div></section><section className="values page-shell">{c.values.map((v, i) => <article key={i}><span>{String(i + 1).padStart(2, "0")}</span><h3>{v.title}</h3><p>{v.copy}</p></article>)}</section><section className="about-cta page-shell"><p>{c.ctaText}</p><Link href="/contact" className="button button-dark">{c.ctaButton} <span>↗</span></Link></section></SiteShell>; }
