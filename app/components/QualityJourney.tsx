"use client";
import { useState } from "react";
import type { SiteContent } from "@/lib/default-content";
export function QualityJourney({ copy }: { copy: SiteContent["quality"] }) { const [active,setActive]=useState(0); const stage=copy.stages[active]; const move=(d:number)=>setActive((v)=>(v+d+copy.stages.length)%copy.stages.length); return <section className="journey-section"><div className="page-shell"><div className="journey-heading"><div><p className="eyebrow">{copy.journeyEyebrow}</p><h2>{copy.journeyTitle}<br /><em>{copy.journeyAccent}</em></h2></div><p>{copy.journeyIntro}</p></div><div className="journey-stepper" role="tablist">{copy.stages.map((item,i)=><button type="button" role="tab" aria-selected={active===i} className={active===i?"active":""} onClick={()=>setActive(i)} key={`${item.title}-${i}`}><span>{String(i+1).padStart(2,"0")}</span><small>{item.phase}</small></button>)}</div><div className="journey-panel" role="tabpanel" aria-live="polite"><div className="journey-stage-id"><span>{copy.stageLabel}</span><strong>{String(active+1).padStart(2,"0")}</strong><small>of {copy.stages.length}</small></div><div className="journey-stage-copy"><p>{stage.phase}</p><h3>{stage.title}</h3><p>{stage.summary}</p><div className="journey-outcome"><span>{copy.releaseLabel}</span><strong>{stage.outcome}</strong></div></div><div className="journey-checks"><span>{copy.checksLabel}</span><ul>{stage.checks.map((check,i)=><li key={i}><b>{String(i+1).padStart(2,"0")}</b>{check}</li>)}</ul></div><div className="journey-controls"><button onClick={()=>move(-1)}>←</button><span>{active+1} / {copy.stages.length}</span><button onClick={()=>move(1)}>→</button></div></div></div></section>; }
